module {
  public let ipfsConfig = {
    apiUrl = "http://localhost:5001/api/v0";
    gateway = "http://localhost:8080/ipfs";
    projectId = "";  // Not needed for local IPFS
    projectSecret = "";  // Not needed for local IPFS
    dedicatedGateway = null;
  };
}
